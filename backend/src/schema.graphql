# import * from './generated/prisma.graphql'

type Mutation {
	createActivity(
		title: String
		description: String
		price: Int
		image: String
		largeImage: String
	): Activity!
}

type Query {
	activities: [Activity]!
}
